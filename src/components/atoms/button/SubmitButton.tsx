import { FaManifyingGlass } from "../../font-icon/FontAwesomeIcons";

export default function SubmitButton(): React.ReactElement {
  return (
    <button type="submit" className="flex items-center justify-center">
      <FaManifyingGlass />
    </button>
  );
}
